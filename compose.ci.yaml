services:
  proxy:
    ports: !reset []
  api:
    environment:
      DEV_AUTH_ENABLED: "true"
      GOOGLE_OAUTH_CLIENT_ID: ""
      GOOGLE_OAUTH_CLIENT_SECRET: ""
      GOOGLE_OAUTH_REDIRECT_URL: ""
